# Local Development Environment Variables
# This file is for LOCAL TESTING ONLY
# In production, these will be set in Vercel's environment variables

# This key must match the fallback key in your api.py for local testing
NEXT_PUBLIC_INTERNAL_API_KEY=a-very-secret-string-for-local-dev

# Email Configuration for Lead Delivery (REQUIRED FOR PRODUCTION)
# Replace with your actual email credentials
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-password
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587